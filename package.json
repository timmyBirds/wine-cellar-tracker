{
  "name": "wine-cellar-grid-ui",
  "version": "1.0.0",
  "description": "Wine Cellar Tracker Grid UI",
  "scripts": {
    "start": "serve -s build",
    "build": "echo 'No build step needed for basic UI'"
  },
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "serve": "^14.0.1"
  }
}
